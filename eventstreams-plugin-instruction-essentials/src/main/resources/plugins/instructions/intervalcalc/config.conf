instructions += {
  name = "Transform: Interval Calculator"
  class = "eventstreams.plugins.essentials.IntervalCalcInstruction"
  config = {
    schema = "/plugins/instructions/intervalcalc/config-schema.json"
  }
}