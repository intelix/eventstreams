instructions += {
  name = "Storage: Persistence"
  class = "eventstreams.plugins.essentials.PersistenceInstruction"
  config = {
    schema = "/plugins/instructions/persistence/config-schema.json"
  }
}