instructions += {
  name = "Transform: Drop field"
  class = "eventstreams.plugins.essentials.DropFieldInstruction"
  config = {
    schema = "/plugins/instructions/dropfield/config-schema.json"
  }
}